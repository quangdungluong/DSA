#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
    int t;
    string s;
    cin >> t;
    while (t--) {
        cin >> s;
        cout << s.size() << '\n';
    }
    return 0;
}