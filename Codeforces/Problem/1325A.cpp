#include <bits/stdc++.h>
using namespace std;

#define ll long long
int main() {
    ll t, x;
    cin >> t;
    while (t--) {
        cin >> x;
        cout << 1 << " " << x - 1 << '\n';
    }
    return 0;
}